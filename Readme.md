To run the project cd backend
npm install
node migrate.js
npm start



cd frontend
npm install
npm run dev
# Project Setup

## Prerequisites

Make sure you have installed:

- Node.js (v18+ recommended)
- npm

---

## Backend Setup

Navigate to the backend directory:

```bash
cd backend
```

Install dependencies:

```bash
npm install
```

Run database migrations:

```bash
node migrate.js
```

Start the backend server:

```bash
npm start
```

---

## Frontend Setup

Open a new terminal and navigate to the frontend directory:

```bash
cd frontend
```

Install dependencies:

```bash
npm install
```

Start the development server:

```bash
npm run dev
```

---

## Running the Application

1. Start the backend server.
2. Start the frontend development server.
3. Open the frontend URL displayed in the terminal (typically `http://localhost:5173`).

## Project Structure

```text
project-root/
├── backend/
│   ├── migrate.js
│   └── ...
├── frontend/
│   └── ...
└── README.md
```
